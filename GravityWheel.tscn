[gd_scene load_steps=5 format=2]

[ext_resource path="res://ASSETS/SPRITES/GravityCircleArrow.png" type="Texture" id=1]
[ext_resource path="res://GravityWheel.cs" type="Script" id=2]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform float angle;
uniform float timeAngle = 0;

void fragment()
{
	vec2 centeredUV = UV - vec2(0.5);
	float radiusSquared = centeredUV.x*centeredUV.x + centeredUV.y*centeredUV.y;
	if (radiusSquared > 0.07 || radiusSquared < 0.065)
	{
		COLOR = vec4(0);
	}
	
	float timeTheta = atan(centeredUV.x, centeredUV.y) + 3.14;
	if (timeTheta > 6.28 - timeAngle && radiusSquared < 0.065)
	{
		COLOR = vec4(1, 0, 0, 1);
	}
	
}"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )
shader_param/angle = 0.0
shader_param/timeAngle = 0.0

[node name="GravityWheel" type="Node2D"]
script = ExtResource( 2 )

[node name="ColorRect" type="ColorRect" parent="."]
material = SubResource( 2 )
anchor_right = 1.0
anchor_bottom = 1.0
margin_left = 420.0
margin_right = 1500.0
margin_bottom = 1080.0

[node name="GravityCircleArrow" type="Sprite" parent="."]
position = Vector2( 960, 540 )
texture = ExtResource( 1 )

[node name="Timer" type="Timer" parent="."]
wait_time = 0.2
one_shot = true

[connection signal="timeout" from="Timer" to="." method="sig_TimerTimeout"]
